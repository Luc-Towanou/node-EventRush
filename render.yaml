# //render.yaml
services:
  - type: web
    name: node-EventRush
    env: node
    plan: free
    buildCommand: |
      npm install
      npx prisma generate
      npm run build
    startCommand: npm run start:prod
    envVars:
      - key: DATABASE_URL
        sync: false   # tu le définis dans Render Dashboard
